services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: natsu_development
      POSTGRES_USER: natsu
      POSTGRES_PASSWORD: natsu
    ports:
      - "5432:5432"
    volumes:
      - natsu_postgres_data:/var/lib/postgresql/data

volumes:
  natsu_postgres_data:
